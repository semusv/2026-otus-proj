# Бэклог (вне этапов 9–11 PLAN.md)

> Отложенные идеи этапа 8 и последующих наблюдений. Этапы 9 (E2E+нагрузка), 10 (minikube),
> 11 (финализация) остаются приоритетом — сюда заглядывать между ними.
> Формат: P1 — сделать следующим полезным шагом, P2 — заметно улучшает демо/UX, P3 — по остаточному принципу.

## Frontend

| Pri | Идея | Зачем / что нужно |
|-----|------|-------------------|
| ~~P1~~ | ~~**Просмотр источника по клику**: текст акта/чанка в модалке~~ | ~~Реализуется в фиче C: клик по citation-card → Modal с метаданными акта + full_text~~ |
| ~~P1~~ | ~~**C.1 — Delete пользователя** в AdminPage: кнопка «Удалить» + `confirm()` (soft/hard) + inactive-стилизация~~ | ~~Фронтенд-обёртка над `DELETE /admin/users/{id}?hard=true|false`~~ |
| ~~P1~~ | ~~**C.2 — Модалка с текстом акта**: клик по citation-card → `<Modal>` + `GET /acts/{act_id}/content` (title, doc_number, date, status, clearance, full_text)~~ | ~~UX: полный текст источника без перехода со страницы~~ |
| ~~P1~~ | ~~**C.3 — Реэкспорт OpenAPI + регенерация api-types.ts**~~ | ~~`make openapi-export && make openapi-types` — синхронизация контракта~~ |
| ~~P1~~ | ~~**C.4 — Смена грифа акта в AdminPage**: поиск по ID + кнопки PUBLIC/INTERNAL/SECRET~~ | ~~Фронтенд-обёртка над `PATCH /admin/acts/{id}/clearance`; бэклог PLAN.md п.4~~ |
| P2 | Markdown-рендеринг ответа LLM (`**bold**`, списки, таблицы) | Ответы Qwen приходят с разметкой, сейчас отображается сырой текст |
| P2 | История диалогов: список `chat_sessions` из PG, возобновление/удаление | Данные уже пишутся в PG; нужен read-API + экран со списком |
| P2 | Кнопка копирования ответа/цитаты/trace_id; ссылка на трейс в Jaeger (`jaeger.localhost/search?trace=<id>`) | Удобство разбора инцидентов прямо из UI (правило отладки из контекста PLAN) |
| P2 | Индикатор живости backend (`/health`) в хедере | Видно причину «сервер недоступен» без консоли |
| P3 | Автоскролл-тоггл в чате (не дёргать ленту при стриминге, если пользователь читает выше) | UX длинных ответов |
| P3 | Переключатель темы (сейчас фиксированная тёмно-синяя), компактный/широкий режим | Косметика |

## Backend / API

| Pri | Идея | Зачем / что нужно |
|-----|------|-------------------|
| P1 | **Прогресс ingestion**: `processed_acts/files_total` в статусе или SSE-канал прогресса; персистентность статуса прогона в PG | Статус in-memory (рестарт сбрасывает в idle), прогресс виден только в логах; UI показывает лишь таймер |
| P2 | Отмена прогона (`DELETE /admin/ingest`) + инкрементальный режим (только новые XML) | Полный перегон corpus_test ~10–30 мин; иногда нужно остановить/догрузить || P2 | Rate-limit `/auth/login` (brute-force) + блокировка после N неудач | Security-by-Design; данные отказов уже пишутся в audit_log |
| P2 | История `GET /admin/stats` во времени (Prometheus gauge + панель Grafana «рост базы») | Демо динамики: было/стало после прогона |
| P3 | Refresh-токен / тихое продление сессии по активности | Сейчас релогин каждые `APP_JWT_TTL_MINUTES` |
| P3 | Экспорт диалога (Markdown) по session_id | Для отчётов/приложения к домашке |

## Quality / DevEx

| Pri | Идея | Зачем / что нужно |
|-----|------|-------------------|
| P1 | **Playwright E2E демо-сценария** против compose (login → стрим → цитаты → RBAC) | Автоматизирует ручную браузерную приёмку из README |
| P2 | pre-commit хуки фронта (eslint/prettier on staged) | Паритет с бэкендом (ruff+mypy уже в pre-commit) |
| P2 | CI-заготовка: гейты (backend lint/unit, frontend lint/test/build, newman smoke) на push | Сейчас всё локально; GitHub Actions/Gitea — по обстановке доступа |
| P3 | Storybook-подобная витрина компонентов (чипы стадий, бейджи clearance) | Быстрая правка визуала без полного стека |
